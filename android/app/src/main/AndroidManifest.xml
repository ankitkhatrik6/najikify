<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network permissions for LAN discovery, HTTP streaming, and WebSockets -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>

    <!--
      Storage permissions.

      Najikify only ever writes to the app's own external files directory
      (Android/data/com.najikify.app/files/Download/Najikify, resolved through
      path_provider) and picks files through the Storage Access Framework
      (file_selector), which grants access per-URI at runtime.

      That means none of the broad storage permissions below are needed on
      modern Android. They were previously declared (MANAGE_EXTERNAL_STORAGE,
      READ_MEDIA_*, requestLegacyExternalStorage) and made Najikify look like a
      "all files access" app to Google Play Protect, which contributed to the
      "Play Protect hasn't seen an app from this developer before" block.
      Only the legacy permissions required on Android 9 and older are kept.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>

    <!-- Camera permission for scanning pairing QR codes -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- Prevent system sleep during large file transfers -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Android 13+ runtime permission, needed to post the "update available"
         notification (see NotificationGateway / MainActivity). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Restores the periodic background update check after a reboot: the
         JobScheduler job is persisted (JobInfo.setPersisted(true)), which
         requires this permission. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="Najikify"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link intent filter for QR code pairing -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="najikify" android:host="pair"/>
            </intent-filter>
        </activity>
        <!-- Periodic background release check. It posts the "update available"
             notification on its own — no need to open Najikify to learn about a
             new version. See UpdateCheckJobService / UpdateNotifier. -->
        <service
            android:name=".UpdateCheckJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
